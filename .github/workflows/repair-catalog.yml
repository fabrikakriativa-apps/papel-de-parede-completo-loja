name: Legacy repair disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy repair is intentionally disabled
        run: |
          echo "The historical 1,207-item repair pipeline is disabled."
          echo "Use the versioned Revision 07 publication workflow instead."
          echo "This guard prevents older repair logic from overwriting the current catalog."
